这个功能模块是关于导码的部分。
导码使用到的技术栈包括：
1.同库，多表。这里使用到了sharding分片策略
2.多线程。多线程主要是为了提高其导入的效率。
2.1这里用到了线程池，按照没20万条码记录进行多线程的异步处理。
2.2这里使用了，callable的多线程实现方式。（原因是因为需要返回插入失败的错误码，并统一写入到错误码文件中）
3.文件流。因为导码是需要导入文件的。通过特定的方式进行解析
好了，就让我们进入到开心而收获满满的正文中来吧。
